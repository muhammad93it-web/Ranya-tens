kind = "web"
previewPath = "/"
title = "Tennis Ranya"
version = "1.0.0"
id = "artifacts/tennis-ranya"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21820

[services.development]
run = "pnpm --filter @workspace/tennis-ranya run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/tennis-ranya", "run", "build" ]
publicDir = "artifacts/tennis-ranya/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21820"
BASE_PATH = "/"
